{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "shay:oasf-descriptor",
  "title": "OASF Descriptor",
  "description": "Minimal Open Agentic Schema Framework (OASF) descriptor. Captures the identity and interface contract of an agentic capability.",
  "type": "object",
  "required": ["id", "version", "name", "description"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this capability"
    },
    "version": {
      "type": "string",
      "description": "Semantic version string"
    },
    "name": {
      "type": "string",
      "description": "Human-readable name"
    },
    "description": {
      "type": "string",
      "description": "Detailed description of what this capability provides"
    },
    "permissions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional list of required permissions"
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional list of capability IDs this depends on"
    },
    "entrypoint": {
      "type": "string",
      "description": "Optional path to the main export file"
    }
  },
  "additionalProperties": false
}
